namespace DeadfireAbilityExtractor
{
    public enum LearnType { None, Automatic, Optional, Mixed };
    public enum ActivationType { None, Active, Passive, Modal };
    public enum RestorationType { None, Encounter, Rest };
    public enum AbilityOrigin { None, Class, Character, Equipment, Consumable, Creature, World, Quest };
}
